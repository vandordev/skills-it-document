# Install For Claude

1. Copy `adapters/claude/CLAUDE.md` and the adjacent `references/` directory into the instruction location you use for Claude to install `it-document`.
2. Preserve relative paths so the references remain available beside `CLAUDE.md`.
3. Reload the workspace or restart the Claude session before use.
