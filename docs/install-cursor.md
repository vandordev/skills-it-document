# Install For Cursor

1. Copy `adapters/cursor/AGENTS.md` and the adjacent `references/` directory into the project root used by Cursor to install `it-document`.
2. Preserve the file name `AGENTS.md`.
3. Reload the workspace or restart Cursor before using the adapter.
