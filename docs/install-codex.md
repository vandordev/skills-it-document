# Install For Codex

1. Copy `adapters/codex/` into your Codex skills directory as `it-document/`.
2. Keep `SKILL.md`, `agents/openai.yaml`, and `references/` together.
3. Restart or reload Codex so the skill is rediscovered.
